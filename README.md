# COMMANDS

## INSTALLATION

```sh
npm install --global yarn
yarn --version
yarn install
yarn start
```

## UPDATE PACKAGES

```sh
npx npm-check-updates -u

yarn upgrade-interactive --latest
yarn install
```
